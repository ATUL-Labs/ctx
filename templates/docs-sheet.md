---
stack: <name, e.g. laravel>
version: "<major.minor the sheet was verified against, e.g. 12.x>"
fetched: YYYY-MM-DD
source: <docs URL or MCP name>
---

# <Topic> - <stack> <version>

Keep the whole sheet under 300 lines. Exact signatures only - if you did not
verify it against the fetched docs this session, it does not go in the sheet.

## Signatures

Copy-pasteable, with parameter and return types. One blank line between entries.

## Gotchas

Version-specific behavior that differs from what training data assumes.
Each entry: one bold line stating the trap, 1-3 lines on the correct form.

## Near-misses

Mistakes an agent almost made that this sheet exists to prevent. Include the
wrong form and the right form so future FTS queries match both spellings.
